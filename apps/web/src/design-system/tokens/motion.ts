export const motion = {
  duration: {
    press: "var(--duration-press)",
    hover: "var(--duration-hover)",
    overlay: "var(--duration-overlay)",
    entrance: "var(--duration-entrance)",
  },
  staggerDelay: "var(--stagger-delay)",
  easing: {
    default: "var(--easing-default)",
  },
} as const;
