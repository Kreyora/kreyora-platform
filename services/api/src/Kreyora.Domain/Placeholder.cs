namespace Kreyora.Domain;

/// <summary>
/// Placeholder to keep the Domain assembly non-empty.
/// Business entities will be added in later milestones.
/// </summary>
public static class DomainAssemblyMarker
{
    public static readonly string AssemblyName = typeof(DomainAssemblyMarker).Assembly.GetName().Name!;
}
